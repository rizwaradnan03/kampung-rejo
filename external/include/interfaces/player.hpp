#ifndef PLAYER_INTERFACE_HPP
#define PLAYER_INTERFACE_HPP

#include <string>
#include <vector>
#include <utility>

struct PlayerInterface{
    int id;
    std::string name;
    std::string color;
    float width;
    float height;
};


#endif